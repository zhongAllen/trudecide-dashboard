# REQ-180: 资金流向模块

## 需求概述
采集多源资金流向数据，包括个股、板块、行业、大盘等多维度资金流向。

## 业务价值
- 支持资金流向分析
- 追踪主力资金动向
- 辅助投资决策

## 数据表设计

### moneyflow - 个股资金流向（Tushare标准）
```sql
CREATE TABLE moneyflow (
    ts_code VARCHAR(20),
    trade_date DATE,
    buy_sm_vol BIGINT,
    buy_sm_amount DECIMAL(20,2),
    sell_sm_vol BIGINT,
    sell_sm_amount DECIMAL(20,2),
    buy_md_vol BIGINT,
    buy_md_amount DECIMAL(20,2),
    sell_md_vol BIGINT,
    sell_md_amount DECIMAL(20,2),
    buy_lg_vol BIGINT,
    buy_lg_amount DECIMAL(20,2),
    sell_lg_vol BIGINT,
    sell_lg_amount DECIMAL(20,2),
    buy_elg_vol BIGINT,
    buy_elg_amount DECIMAL(20,2),
    sell_elg_vol BIGINT,
    sell_elg_amount DECIMAL(20,2),
    net_mf_vol BIGINT,
    net_mf_amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (ts_code, trade_date)
);
```

### moneyflow_ths - 个股资金流向（同花顺）
```sql
CREATE TABLE moneyflow_ths (
    ts_code VARCHAR(20),
    trade_date DATE,
    buy_amount DECIMAL(20,2),
    sell_amount DECIMAL(20,2),
    net_amount DECIMAL(20,2),
    main_buy_amount DECIMAL(20,2),
    main_sell_amount DECIMAL(20,2),
    main_net_amount DECIMAL(20,2),
    retail_buy_amount DECIMAL(20,2),
    retail_sell_amount DECIMAL(20,2),
    retail_net_amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (ts_code, trade_date)
);
```

### moneyflow_dc - 个股资金流向（东方财富）
```sql
CREATE TABLE moneyflow_dc (
    ts_code VARCHAR(20),
    trade_date DATE,
    buy_amount DECIMAL(20,2),
    sell_amount DECIMAL(20,2),
    net_amount DECIMAL(20,2),
    main_buy_amount DECIMAL(20,2),
    main_sell_amount DECIMAL(20,2),
    main_net_amount DECIMAL(20,2),
    retail_buy_amount DECIMAL(20,2),
    retail_sell_amount DECIMAL(20,2),
    retail_net_amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (ts_code, trade_date)
);
```

### moneyflow_industry_ths - 行业/板块资金流向（同花顺）
```sql
CREATE TABLE moneyflow_industry_ths (
    code VARCHAR(20),
    name VARCHAR(100),
    trade_date DATE,
    buy_amount DECIMAL(20,2),
    sell_amount DECIMAL(20,2),
    net_amount DECIMAL(20,2),
    main_buy_amount DECIMAL(20,2),
    main_sell_amount DECIMAL(20,2),
    main_net_amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (code, trade_date)
);
```

### moneyflow_industry_dc - 板块资金流向（东方财富）
```sql
CREATE TABLE moneyflow_industry_dc (
    code VARCHAR(20),
    name VARCHAR(100),
    trade_date DATE,
    buy_amount DECIMAL(20,2),
    sell_amount DECIMAL(20,2),
    net_amount DECIMAL(20,2),
    main_buy_amount DECIMAL(20,2),
    main_sell_amount DECIMAL(20,2),
    main_net_amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (code, trade_date)
);
```

### moneyflow_market_dc - 大盘资金流向（东方财富）
```sql
CREATE TABLE moneyflow_market_dc (
    trade_date DATE PRIMARY KEY,
    sh_buy_amount DECIMAL(20,2),
    sh_sell_amount DECIMAL(20,2),
    sh_net_amount DECIMAL(20,2),
    sz_buy_amount DECIMAL(20,2),
    sz_sell_amount DECIMAL(20,2),
    sz_net_amount DECIMAL(20,2),
    total_buy_amount DECIMAL(20,2),
    total_sell_amount DECIMAL(20,2),
    total_net_amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW()
);
```

## Tushare接口映射

| 数据表 | Tushare接口 | 积分消耗 | 更新频率 |
|--------|-------------|----------|----------|
| moneyflow | moneyflow | 免费 | 每日 |
| moneyflow_ths | moneyflow_ths | 2000/次 | 每日 |
| moneyflow_dc | moneyflow_dc | 2000/次 | 每日 |
| moneyflow_industry_ths | moneyflow_industry_ths | 2000/次 | 每日 |
| moneyflow_industry_dc | moneyflow_industry_dc | 2000/次 | 每日 |
| moneyflow_market_dc | moneyflow_market_dc | 2000/次 | 每日 |

## 采集脚本
- `collect_moneyflow.py`
- `collect_moneyflow_ths.py`
- `collect_moneyflow_dc.py`
- `collect_moneyflow_industry.py`

## 验收标准
- [ ] 所有表结构创建成功
- [ ] 采集脚本运行正常
- [ ] 数据能正常写入
- [ ] collect_target已注册

## 版本
v1.5.2
