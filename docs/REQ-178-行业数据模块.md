# REQ-178: 行业数据模块

## 需求概述
采集申万和中信行业分类数据，包括行业分类、成分股、行业指数行情等。

## 业务价值
- 支持行业分析和对比
- 为板块轮动策略提供数据基础
- 支持行业成分股筛选

## 数据表设计

### sw_industry_classify - 申万行业分类
```sql
CREATE TABLE sw_industry_classify (
    code VARCHAR(20) PRIMARY KEY,
    name VARCHAR(100),
    industry_type VARCHAR(20),
    level VARCHAR(10),
    parent_code VARCHAR(20),
    is_valid BOOLEAN DEFAULT true,
    collected_at TIMESTAMPTZ DEFAULT NOW()
);
```

### sw_industry_member - 申万行业成分
```sql
CREATE TABLE sw_industry_member (
    index_code VARCHAR(20),
    con_code VARCHAR(20),
    con_name VARCHAR(100),
    in_date DATE,
    out_date DATE,
    is_new BOOLEAN DEFAULT true,
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (index_code, con_code, in_date)
);
```

### sw_industry_daily - 申万行业指数日行情
```sql
CREATE TABLE sw_industry_daily (
    ts_code VARCHAR(20),
    trade_date DATE,
    open DECIMAL(10,2),
    high DECIMAL(10,2),
    low DECIMAL(10,2),
    close DECIMAL(10,2),
    pre_close DECIMAL(10,2),
    change DECIMAL(10,2),
    pct_chg DECIMAL(10,4),
    vol BIGINT,
    amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (ts_code, trade_date)
);
```

### citic_industry_classify - 中信行业分类
```sql
CREATE TABLE citic_industry_classify (
    code VARCHAR(20) PRIMARY KEY,
    name VARCHAR(100),
    industry_type VARCHAR(20),
    level VARCHAR(10),
    parent_code VARCHAR(20),
    is_valid BOOLEAN DEFAULT true,
    collected_at TIMESTAMPTZ DEFAULT NOW()
);
```

### citic_industry_member - 中信行业成分
```sql
CREATE TABLE citic_industry_member (
    index_code VARCHAR(20),
    con_code VARCHAR(20),
    con_name VARCHAR(100),
    in_date DATE,
    out_date DATE,
    is_new BOOLEAN DEFAULT true,
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (index_code, con_code, in_date)
);
```

### citic_industry_daily - 中信行业指数日行情
```sql
CREATE TABLE citic_industry_daily (
    ts_code VARCHAR(20),
    trade_date DATE,
    open DECIMAL(10,2),
    high DECIMAL(10,2),
    low DECIMAL(10,2),
    close DECIMAL(10,2),
    pre_close DECIMAL(10,2),
    change DECIMAL(10,2),
    pct_chg DECIMAL(10,4),
    vol BIGINT,
    amount DECIMAL(20,2),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (ts_code, trade_date)
);
```

### global_index - 国际主要指数
```sql
CREATE TABLE global_index (
    ts_code VARCHAR(20) PRIMARY KEY,
    name VARCHAR(100),
    market VARCHAR(50),
    category VARCHAR(50),
    collected_at TIMESTAMPTZ DEFAULT NOW()
);
```

### global_index_daily - 国际指数日线
```sql
CREATE TABLE global_index_daily (
    ts_code VARCHAR(20),
    trade_date DATE,
    open DECIMAL(10,2),
    high DECIMAL(10,2),
    low DECIMAL(10,2),
    close DECIMAL(10,2),
    pre_close DECIMAL(10,2),
    change DECIMAL(10,2),
    pct_chg DECIMAL(10,4),
    collected_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (ts_code, trade_date)
);
```

## Tushare接口映射

| 数据表 | Tushare接口 | 积分消耗 | 更新频率 |
|--------|-------------|----------|----------|
| sw_industry_classify | index_classify | 免费 | 月度 |
| sw_industry_member | index_member | 免费 | 月度 |
| sw_industry_daily | index_daily | 免费 | 每日 |
| citic_industry_classify | index_classify | 免费 | 月度 |
| citic_industry_member | index_member | 免费 | 月度 |
| citic_industry_daily | index_daily | 免费 | 每日 |
| global_index | index_global | 免费 | 每日 |
| global_index_daily | index_global | 免费 | 每日 |

## 采集脚本
- `collect_sw_industry.py`
- `collect_citic_industry.py`
- `collect_global_index.py`

## 验收标准
- [ ] 所有表结构创建成功
- [ ] 采集脚本运行正常
- [ ] 数据能正常写入
- [ ] collect_target已注册

## 版本
v1.5.2
